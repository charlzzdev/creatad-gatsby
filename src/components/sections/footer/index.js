import React from 'react';

import './index.scss';

const Footer = () => {
  return (
    <footer>
      © {new Date().getFullYear()} CreatAd
    </footer>
  );
};

export default Footer;
